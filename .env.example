# No credentials needed. Auth lives in your signed-in zillow.com browser
# tab via the fetchproxy extension. This file is a marker only.
#
# Optional overrides:
# ZILLOW_WS_PORT=37149           # fetchproxy WebSocket port
